apiVersion: v1
kind: Service
metadata:
  name: kguard-ai
  namespace: k-guard
  labels:
    app: kguard-ai
spec:
  selector:
    app: kguard-ai
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
